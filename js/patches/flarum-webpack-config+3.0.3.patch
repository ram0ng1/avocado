diff --git a/node_modules/flarum-webpack-config/src/OverrideChunkLoaderFunction.cjs b/node_modules/flarum-webpack-config/src/OverrideChunkLoaderFunction.cjs
index 5de8609..e263b58 100644
--- a/node_modules/flarum-webpack-config/src/OverrideChunkLoaderFunction.cjs
+++ b/node_modules/flarum-webpack-config/src/OverrideChunkLoaderFunction.cjs
@@ -22,11 +22,15 @@ class OverrideChunkLoaderFunction {
         );
       });
 
-      // log webpack runtime after the final code, the hook isn't requireEnsure
+      // log webpack runtime after the final code — use new webpack 5 API to avoid deprecation warning
       if (namespace !== 'core') {
-        compilation.mainTemplate.hooks.require.tap('OverrideChunkLoaderFunction', (source) => {
-          return 'flarum.reg._webpack_runtimes["' + namespace + '"] ||= __webpack_require__;' + source;
-        });
+        const { JavascriptModulesPlugin } = require('webpack').javascript;
+        JavascriptModulesPlugin.getCompilationHooks(compilation).renderRequire.tap(
+          'OverrideChunkLoaderFunction',
+          (source) => {
+            return 'flarum.reg._webpack_runtimes["' + namespace + '"] ||= __webpack_require__;' + source;
+          }
+        );
       }
     });
   }
diff --git a/node_modules/flarum-webpack-config/src/autoExportLoader.cjs b/node_modules/flarum-webpack-config/src/autoExportLoader.cjs
index 8f3e914..b30e88d 100644
--- a/node_modules/flarum-webpack-config/src/autoExportLoader.cjs
+++ b/node_modules/flarum-webpack-config/src/autoExportLoader.cjs
@@ -24,7 +24,7 @@ let namespace;
 function addAutoExports(source, pathToModule, moduleName) {
   let addition = '';
 
-  const defaultExportMatches = [...source.matchAll(/export\s+?default\s(?:abstract\s)?(?:async\s)?(?:(?:function|abstract|class)\s)?([A-Za-z_]*)/gm)];
+  const defaultExportMatches = [...source.matchAll(/export\s+?default\s(?:abstract\s)?(?:async\s)?(?:(?:function|abstract|class)\s)?([A-Za-z_$][A-Za-z0-9_$]*)/gm)];
   const defaultExport = defaultExportMatches.length ? defaultExportMatches[0][1] : null;
 
   // In case of an index.js file that exports multiple modules
@@ -94,7 +94,7 @@ function addAutoExports(source, pathToModule, moduleName) {
       //      these can be `export function|class|enum|.. Name ..`
       //      or `export { ... };
       {
-        const matches = [...source.matchAll(/export\s+?(?:\* as|function|{\s*([A-z0-9, ]+)+\s?}|const|let|abstract\s?|class)+?\s?([A-Za-z_]*)?/gm)];
+        const matches = [...source.matchAll(/export\s+?(?:\* as|function|{\s*([A-z0-9, ]+)+\s?}|const|let|abstract\s?|class)+?\s?([A-Za-z_$][A-Za-z0-9_$]*)?/gm)];
 
         if (matches.length) {
           const map = matches.reduce((map, match) => {
